#!/bin/sh
#
# Объявление проекта: у продукта свой безопасный запуск тестов.
#
# Приложение живёт на той же машине: у него есть установленная копия,
# глобальная горячая клавиша и пользовательское хранилище. Обычный
# «xcodebuild test» их трогает; этот запуск — нет.
exec zsh scripts/test-safely.sh -enableCodeCoverage YES "$@"
