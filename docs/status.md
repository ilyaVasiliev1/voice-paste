# Состояние

Здоровье, блокеры и зона удалённого. Структура — `map.md`, решения — `decisions.md`.

Числа здесь — измеренные. Не мерил — так и написано.

## Замеры от 2026-09-20

Среда: Xcode 26.6 (build 17F113), Swift 6.3.3, macOS 15+ на Apple Silicon.

| Что | Результат | Чем мерил |
|---|---|---|
| Сборка | `BUILD SUCCEEDED`, 1 предупреждение | `xcodebuild -scheme VoicePaste -destination 'platform=macOS' build` |
| Тесты | 237 прошли, 0 упали | `zsh scripts/test-safely.sh` |
| Линтер | 27 нарушений, все в базовой линии, новых 0 | `swiftlint --strict --baseline .swiftlint-baseline.json` |
| Мёртвый код | сверх базовой линии нет | `periphery scan` (команда ниже) |
| Спецификация | 132 блока: 90 доказаны, 42 без доказательства, пробелов 0, вне спецификации 0 | `python3 ~/Documents/Depo/plugins/depo/bin/spec-check.py .` |

Тесты прогонялись при снесённом `.tmp/` и без сети — кэш пакетов лежит вне
репозитория, см. `../CLAUDE.md`.

`periphery` ведёт собственный каталог сборки и мимо кэша уходит в сеть. Чтобы не
уходил, кэш передаётся ему аргументами сборки:

```bash
periphery scan --project VoicePaste.xcodeproj --schemes VoicePaste \
  --exclude-tests --retain-swift-ui-previews \
  --disable-redundant-public-analysis --disable-update-check --quiet \
  --baseline .periphery-baseline.json \
  -- -clonedSourcePackagesDirPath "$HOME/Library/Caches/VoicePaste/SourcePackages" \
     -disableAutomaticPackageResolution
```

## Скорость распознавания

Замер 2026-09-20 на установленной модели `large-v3` и реальной записи в 132 с.
Прогрев (3 с речи) — 1,52 с на холодной модели и 0,88 с на уже скомпилированной;
в таблицу не входит.

Прогонялся дважды: отрезки 60 и 120 с совпали до сотой секунды, короткие
разошлись на 2–4 %. Числа ниже — из первого прогона.

| речь, с | счёт, с | доля реального времени |
|---|---|---|
| 5 | 0,83 | 0,166 |
| 15 | 1,38 | 0,092 |
| 28 | 2,25 | 0,080 |
| 60 | 4,66 | 0,078 |
| 120 | 8,12 | 0,068 |

Отсюда: постоянная накладная около **0,51 с на вызов** плюс около **0,063 с на
секунду речи**. Доля реального времени падает с длиной именно потому, что
накладная размазывается.

Что из этого следует для параллельного счёта во время записи:

- Короткая диктовка (до одного окна) от него не выиграет, а проиграет:
  ожидание и так 0,8 с, а разрезание на окна заставит платить накладную
  несколько раз вместо одного.
- Длинная запись: после остановки досчитывается только незакрытый хвост, а он
  не длиннее одного окна. Для записи в 120 с ожидание падает с 8,1 с до
  величины между 0,5 с (хвост только что закрылся) и 2,3 с (окно почти полное).
  Для записи в 60 с — с 4,7 с до того же диапазона: он зависит не от длины
  записи, а от того, где её застала остановка.
- Общий расход процессора при этом **вырастет** (накладная за каждое окно), но
  уйдёт под время записи. Размен: больше работы и батареи ради меньшего ожидания.
- Риск для самой записи низкий и измеренный: окно в 28 с считается 2,25 с, то
  есть распознавание занимает около 8 % времени — конкуренции с захватом звука
  почти нет.

Воспроизвести: см. заголовок `VoicePasteTests/TranscriptionSpeedBenchmark.swift`.
В обычный прогон замер не входит.

## Блокеры

Найдены аудитом при заведении в Depo 2026-09-20, подтверждены чтением кода.

### Открытые

| Что | Где | Цена |
|---|---|---|
| Скачанная модель не проверяется ни хешем, ни подписью; проверяется только наличие ожидаемого файла | `VoicePaste/Core/Transcription/GitHubModelDownloader.swift:207-231` | Единственный сетевой путь офлайн-продукта без защиты целостности. Приложение не в песочнице |
| Задачи очереди импорта с нераспознанным состоянием молча отбрасываются при восстановлении | `VoicePaste/Data/ImportQueueStore.swift:57-77` | Задача исчезает после перезапуска без следа |
| Отключение микрофона во время записи не обрабатывается: наблюдателя смены аудио-графа нет ни одного | `VoicePaste/Core/Audio/AudioCaptureService.swift` | Запись продолжает выглядеть идущей; на выходе тишина либо падение движка |
| Буфер обмена затирается без возврата прежнего содержимого | `VoicePaste/Core/Insertion/TextInserter.swift:164-168` | Чужие данные в буфере теряются безвозвратно |

### Найдены ревью и закрыты 2026-09-20

Ревью тремя линзами по коду, написанному в тот же день. Три из четырёх —
критические, и все три в новом коде.

| Что было | Чем закрыто |
|---|---|
| Отмена насоса не дожидалась зависшего распознавания: вернувшееся окно фиксировалось в уже переустановленном планировщике и роняло процесс по `precondition`. Ловилось обычной остановкой записи в момент счёта окна | Проверка отмены перенесена до фиксации. Результат отменённого окна выбрасывается |
| Горячая клавиша диктовки во время лекции рушила обе записи: микрофон один, `start()` молча возвращался на идущем захвате, `stop()` выгребал буфер лекции. Лекция выглядела записывающейся при остановленном микрофоне | Диктовка отказывает при идущей лекции и объясняет отказ. Обратный запрет тоже объяснён, а не молчалив |
| Отказ модели на остановке лекции выходил из функции до сохранения — всё прослушанное исчезало при следующем старте | Абзацы сохраняются и без хвоста, отказ виден на экране |
| Фильтр повторов на стыке окон отбрасывал сегмент целиком, если тот начинался до границы. Модель часто склеивает повтор с новыми словами в один сегмент — терялись новые слова | Отбрасывается только сегмент, целиком лежащий в принятом времени. Размен сделан в сторону повтора: не увидеть слово хуже, чем увидеть дважды |
| Статистика подменяла отказ базы пустыми данными — неотличимо от «записей нет» | Отказ показывается отдельным состоянием, причина в журнале |

### Закрытые ранее 2026-09-20

| Что было | Чем закрыто |
|---|---|
| Любая ошибка домена `com.apple.CoreML` считалась признаком битых файлов и вела к удалению модели. Нехватка памяти даёт ошибку того же домена, а на пути выполнения сеть запрещена — модель сносилась и не восстанавливалась | Домен перестал быть приговором: решает только текст ошибки. `compiling`/`compilation` убраны из признаков порчи — той же формулировкой Core ML отвечает на нехватку ресурсов. Четыре новых теста |
| Расшифровка после диктовки писалась в историю через `try?`: отказ не видели ни пользователь, ни журнал, HUD показывал успех | Отказ хранилища по-прежнему не отменяет удачную диктовку, но уходит в журнал как `dictation.historySaveFailed` — как это давно сделано в импорте |
| Правка в истории: запись через `try?`, а `onChange` вызывался безусловно — экран показывал сохранённое, в базе оставалось старое | Модель обновляется только после удачной записи. В детали видна пометка «Правка не сохранена», причина в журнале |
| Раскладка главного окна ломалась при растягивании боковой панели: 680 при колонках от 240 и от 520 | Минимум окна выводится из ширин колонок, а не задаётся отдельно. Четыре теста на чистых константах |

## Дизайн: что осталось — с 2026-09-21

Порядок согласован с владельцем. Сначала движок (сделано: лекция идёт на потоковом
распознавателе системы, диктовка остаётся на Whisper), потом навигация, потом стиль.
Плашку диктовки (HUD) не трогать — ею владелец доволен.

### 1. Видеть экран до правки — не сделано до конца

Без этого правка дизайна идёт вслепую. Владелец прямо потребовал проверять
интерфейс снимками, как веб проверяется скриншотами.

- **Сначала попробовать то, что уже стоит:** Peekaboo и навык `macos-ios-qa`
  (поставлены 21.09 в соседней сессии). Снимают настоящее окно запущенного
  приложения. Права на запись экрана у них выданы.
- **Незакоммиченная попытка сделать то же тестом** — `VoicePasteTests/ScreenSnapshots.swift`,
  `scripts/snapshots.sh`, запись в `project.pbxproj`. Тест выводит главное окно
  на временной базе с образцами (история, лекция, статистика) в обеих темах.
  Правая часть окна снимается верно. Левая панель — нет: стекло macOS 26 берёт
  фон из-под окна, и отрисовка внутри процесса даёт белый прямоугольник.
  Системный `screencapture -l` из внешнего наблюдателя отвечает
  «could not create image from window», вероятно потому, что окно вынесено за
  экран на (-10000, -10000). Проверено и не годится: `cacheDisplay` (теряет текст
  SwiftUI), `ImageRenderer` (выпадающие списки заглушками, прокрутка пустая),
  `CALayer.render` (не видит стекла).
- **Решить:** если Peekaboo снимает настоящее окно — тестовую попытку удалить
  целиком, двух путей к одному не держать. Если нужен повторяемый набор снимков
  без запущенного приложения — доводить тест, поставив окно на экран.

### 2. Дефекты, видимые на снимках

| Что | Где | Цена |
|---|---|---|
| «Время речи: 0 мин» при трёх записях по 6–10 с — меньше минуты округляется в ноль | Статистика | Владелец видел то же «0 минут» в сведениях о лекции. Это расхождение с правдой, а не косметика: чинить тестом сначала |
| Два выпадающих списка без подписей; «По языку» — это выбор движка, и на экране это не сказано | Экран лекции | Выбор движка непонятен |
| Таймер записи стоит в ряду с приглушёнными списками и теряется | Экран лекции во время записи | Не видно, что запись идёт и сколько длится |
| Сохранённые лекции — простые строки без даты и без признака, что на них можно нажать; список стоит справа, а история — в боковой панели | Экран лекции | Это и есть расхождение навигации, которое снимает пункт 3 |
| На 30 дней — 29 нулевых точек и один всплеск | График статистики | При редких данных столбцы читаются лучше |
| При ширине 760 кнопки режутся: «Начать ле…», «Скопирова…» | Шапка экрана лекции | Ряд действий не помещается в среднее окно |

### 3. Навигация в три колонки — не начато

Согласовано: `NavigationSplitView` в три колонки. Разделы с подписями словами —
Лекции, История, Импорт, Статистика. Каждый раздел владеет обеими правыми
колонками (список → деталь). Действия контекстные, в панели инструментов, а не
рядом в шапке. Сейчас лекции живут в детали, а история — в боковой панели:
это и есть то, что владелец назвал «навигация стрёмная».

Это меняет поведение, а не только вид: сначала блоки в `spec/`, потом код
(см. `../CLAUDE.md`). Проверять снимками на каждом шаге.

### 4. Стиль macOS 26 — не начато

Через роль дизайнера (`depo:designer`), после навигации. На полке теперь есть
`liquid-glass-design`, `apple-design`, `ios-hig-design` — прежде считалось, что
навыка под macOS нет, это уже неверно. Сюда же входит несведённая шкала
отступов из раздела «Долг».

## Долг

- **518 вхождений прежней нумерации спецификации** (`L-`, `INV-`, `AT-`, `UI-`,
  `DM-`, `EC-`, `US-`, `TOK-`, `DEP-`, `T-`) в комментариях 56 файлов. Документов
  с такими номерами больше нет. Регуляркой не чинится: вокруг идентификаторов
  написаны объяснения, их надо перечитать и переписать.
- **42 блока спецификации без доказательства.** Три группы: слой SwiftUI не
  тестируется вовсе; поведение, требующее живой системы (разрешения TCC, нехватка
  памяти, настоящий `AVAudioEngine`, ветка видео в декодере); числовые пороги,
  выведенные чтением, а не измерением.
- **Оформление сведено в `VoicePaste/UI/DesignTokens.swift`**, но не везде:
  аудит нашёл пять разных длительностей анимации на пять точек и одну и ту же
  зону приёма файла, реализованную дважды с разошедшимися радиусом, пунктиром
  и цветом. Приведены к токенам HUD, очередь импорта, статистика и экран
  лекции. Шкала отступов (`spacing:` двадцатью разными числами) не сведена —
  это отдельная задача, и трогать её попутно значило бы перебрать весь
  интерфейс без возможности проверить результат тестами.
- Жизненный цикл модели отрисован дважды независимо — в настройках и в
  онбординге. Не пропуск состояния, а дублирование: правка лексики потребует
  не забыть оба места.
- Базовые линии долга: `.swiftlint-baseline.json` — 27 нарушений,
  `.periphery-baseline.json` — 28 неиспользуемых символов.
- `AppState.swift` (868 строк) и `ModelManager.swift` (921 строка) выше мягкого
  потолка в 800 строк. За 20.09.2026 оба подросли — на исправлениях дефектов, нарезке окон и
  учебном режиме, — и базовую линию линтера пришлось пересчитывать четырежды.
  Решение не дробить `AppState` косметически записано в `decisions.md`.
  Это сигнал, а не формальность: **разделение обоих просрочено** и должно идти
  отдельной задачей, а не попутно.

## Зона удалённого

Удалено при заведении в Depo 2026-09-20 сознательно. Не восстанавливать без
нового решения в `decisions.md`.

| Что | Почему убрано | Чем заменено | Когда пересмотреть |
|---|---|---|---|
| `spec/` прежнего вида (`_invariants.md`, `logic.md`, `terms.md` и прочие) | Разошлась с кодом: объявляла выгрузку модели через 10 минут при коде, держащем её постоянно; нумерация в комментариях Swift указывала на чужие атомы | `spec/` выведена заново из кода навыком `spec-miner` | Не восстанавливать. Прежние номера мертвы |
| `memory/decisions.md`, `docs/decisions/` | Часть прежней системы разработки, вывезена вместе с ней | `docs/decisions.md`, ведётся с нуля | Не восстанавливать |
| `tools/` (`verify.sh`, `dod.sh`, `map.sh`, `serve.sh`, `test.sh`, `unserve.sh`) | Гейты прежней системы разработки | `scripts/` и команды из этого файла | Не восстанавливать |
| `.claude/` с 34 ссылками | Все 34 вели в прежнюю систему и после переезда были битыми | `.claude/rules/` со ссылками на полку Depo | — |
| `CLAUDE.md` прежнего вида | Утверждал, что папка является git-репозиторием, хотя она им не была, и велел проверять спеку командой `./orc`, которой не существует | `CLAUDE.md`, написан заново | Не восстанавливать |
| `project.json`, `stack.json`, `delta.config.json`, `ops/`, `project-idea.md`, `design.md`, `.spec-refs-baseline.txt` | Привязка к прежней системе разработки | Паспорт продукта — строка в `~/Documents/Depo/projects/INDEX.md` | Не восстанавливать |

## Не отправлено на GitHub

Рабочее дерево чисто, но локальные коммиты впереди `origin/main`. Последний
коммит на GitHub — от 25.08.2026.

Среди них — фича «запуск при входе» (`LoginItemRegistering.swift`, 12 тестов,
правки настроек), написанная 27.08.2026 и до 20.09.2026 существовавшая только
как несохранённые файлы на диске, в одном экземпляре.
