# VoicePaste

Локальная голосовая диктовка и транскрипция файлов для macOS. Живёт в menu bar, запускается глобальной горячей клавишей, распознаёт речь **офлайн** на Apple Silicon и вставляет готовый текст прямо в активное приложение.

## Возможности

- Диктовка по горячей клавише в любое приложение — текст вставляется в активное поле.
- Транскрипция аудио- и видеофайлов (в том числе голосовых Telegram, OGG/Opus).
- Полностью локально: без сервера, аккаунтов и облака; исходное аудио по умолчанию не сохраняется.
- Локальная история расшифровок с полнотекстовым поиском.
- Русский и английский, авто-определение языка; пунктуация из модели и мягкая локальная орфография.

## Требования

- **macOS 14 (Sonoma)** или новее.
- Mac на **Apple Silicon** (M1 или новее).

## Установка (готовое приложение)

Собирать ничего не нужно.

1. Скачай последнюю версию со страницы [**Releases**](https://github.com/ilyaVasiliev1/voice-paste/releases/latest) — файл `VoicePaste.dmg`.
2. Открой `.dmg` и перетащи **VoicePaste** в папку **Программы**.
3. **Первый запуск** — так как приложение не подписано платным сертификатом Apple, macOS один раз спросит подтверждение:
   - запусти VoicePaste (двойной клик). Появится предупреждение «неизвестный разработчик» — это нормально;
   - открой **Системные настройки → Конфиденциальность и безопасность**, пролистай вниз и нажми **«Всё равно открыть»** напротив VoicePaste, подтверди;
   - либо: правый клик по приложению → **«Открыть»** → **«Открыть»**.

   Это нужно **один раз**. Дальше приложение открывается как обычное.

При **первом запуске** приложение один раз скачает модель распознавания (~626 МБ) — она не входит в загрузку и подтягивается локально в `~/Library/Application Support`. Также попросит доступ к микрофону и Универсальному доступу (см. ниже).

## Сборка из исходников (для разработчиков)

Нужен **Xcode 26+**.

```bash
git clone https://github.com/ilyaVasiliev1/voice-paste.git
cd voice-paste
open VoicePaste.xcodeproj
```

В Xcode выбери схему **VoicePaste** и нажми **Run** (⌘R) — зависимости (WhisperKit, GRDB) подтянутся автоматически по `Package.resolved`.

При первом запуске из исходников Xcode может попросить выбрать команду подписи: в **Signing & Capabilities** выбери свою **Personal Team** и оставь *Automatically manage signing*. Платный аккаунт Apple для этого не нужен.

## Разрешения

- **Микрофон** — чтобы записывать речь.
- **Универсальный доступ (Accessibility)** — чтобы вставлять распознанный текст в активное поле. Без него текст просто копируется в буфер обмена.

Оба разрешения приложение объясняет и запрашивает при первом запуске.

## Приватность

Распознавание, история и словарь работают на устройстве. Сеть используется ровно один раз — для первичной загрузки модели по твоему явному действию. Во время записи и распознавания сеть не нужна.

## Удаление

VoicePaste не устанавливается через App Store, поэтому удаляется вручную. **Перетащить приложение в Корзину недостаточно** — модель распознавания (~600 МБ), история, настройки и кэши лежат отдельно, вне бандла приложения, и остаются на диске.

### Полное удаление одной командой

Из папки проекта:

```bash
bash scripts/uninstall.sh
```

Скрипт закрывает приложение, показывает, что и сколько будет удалено, и после подтверждения сносит **всё**: сам `.app`, данные, модель и кэши. Флаг `--yes` — без вопросов.

### Вручную

Если запускать скрипт не хочешь — то же самое в Терминале:

```bash
# 1. приложение
rm -rf /Applications/VoicePaste.app
# 2. данные + модель (~600 МБ)
rm -rf ~/Library/Application\ Support/VoicePaste
# 3. кэши (в т.ч. скомпилированная CoreML-модель)
rm -rf ~/Library/Caches/VoicePaste
rm -rf ~/Library/Caches/com.ilyavasiliev.voicepaste
rm -rf ~/Library/HTTPStorages/com.ilyavasiliev.voicepaste
# 4. настройки и состояние окон
rm -f  ~/Library/Preferences/com.ilyavasiliev.voicepaste.plist
rm -rf ~/Library/Saved\ Application\ State/com.ilyavasiliev.voicepaste.savedState
```

### Разрешения

Ни скрипт, ни команды не трогают выданные разрешения (macOS не даёт менять их программно). Убери их вручную: **Системные настройки → Конфиденциальность и безопасность → Микрофон** и **Универсальный доступ** — сними VoicePaste.

После этого на Mac не остаётся никаких следов приложения.

## Лицензия

[MIT](LICENSE).
